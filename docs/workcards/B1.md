# 施工卡 B1：琴橋導納 — 頻率無關損耗通道（弦域）

> 建立：2026-08-16
> 收件人：AI 工兵（無本次對話記憶）
> 定位：**新物理實作**。在 `StringModel` 的阻尼律加入第四項——弦端經琴橋流向
> 共鳴板的能量損耗，頻率無關。這是 `docs/BRIDGE_ADMITTANCE_SOURCES.md` 的落地卡，
> 也是 `docs/workcards/B2.md`（阻尼寬頻化收尾）的前置。**B2 已經寫好、且明確假設
> 本卡先做完**——B2 §1 會親自檢查 `decayTimeForFrequency` 的 denominator 是否
> 已經有四項，沒有就會停下不動手。所以本卡完工的判定標準之一就是「B2 讀了會
> 過關」。

---

## 0. 一句話目標

給 `StringModel::decayTimeForFrequency` 加一個頻率無關的第四損耗項
`T·G/(ln(1000)·L)`（`G` = 共鳴板無限板驅動點導納的實部），只接在 Cimbalom/Piano
（`StringModel`／`CimbalomEngine.h`）這條渲染路徑上；**Chromatic 引擎
（`BeamModel`/`PlateModel`/`ChromaticEngine.h`）本卡不動一行**。

---

## 1. 前置條件

- **無前置卡片。** 但工作樹目前有 2026-08-10「阻尼寬頻化」的 unstaged 改動
  （`data/materials.json`、`src/physics/{StringModel,BeamModel,PlateModel,MaterialDB}.h`、
  `src/engines/{Cimbalom,Chromatic}Engine.h`、`tests/*.cpp`）。**這批改動月月已裁決
  保留，B1 疊在它上面做，絕對不准 `git checkout`/`git stash` 掉它。**
- **如何確認前置狀態正確**（必須做，不能只看本卡描述）：
  1. `git status` 應該看到上述檔案是 modified、unstaged。
  2. 打開 `src/physics/StringModel.h`，讀 `decayTimeForFrequency`（目前約在
     46–57 行），denominator 應該已經是 `eta` 版三項
     （`internalFrictionRate(eta,f) + beta_air·f² + gamma_radiation·f`），
     **不是**舊的 `alpha` 常數版。若看到的是 alpha 版，代表工作樹狀態跟本卡
     假設的 2026-08-10 基線不符——停下，回報實際看到的內容，不要照抄本卡的
     行號動手。

---

## 2. 依據文件

| 文件 | 節 | 用途 |
|---|---|---|
| `docs/BRIDGE_ADMITTANCE_SOURCES.md` | 全文，尤其 §2.1（`Y∞` 公式與文獻出處）、§2.2（`α` 推導）、§2.3（轉 `T60` 形式）、§3（量級核對表，C2–C8）、§4（模型抓不到什麼，4 點都要知道）、§5（新增參數表）、§6（引用清單） | 本卡唯一的物理依據來源，公式與常數逐條照抄 |
| `reports/damping_broadband_findings.md` | §3（T60 實測結果，尤其 C2 39s→129s 的發散）、§4（根因：缺頻率無關損耗通道）、§6（選項 C／為何要做 B1） | 理解「為什麼要做這件事」 |
| `docs/RESEARCH_INDEX.md` | §3（依賴關係圖，B1 是 B2/B3/B5/B6 的前置）、§6（觸發哪些 Rule） | 施工順序脈絡 |
| `TODO.md` | 開頭「待辦總表」B 段 B1 條目、A 段 A2/A3 | 裁決脈絡（月月已選 (b)：B1 疊在阻尼寬頻化之上一起收） |
| `ROADMAP_PHYSICS.md` | §1（十條規則全文）、§6（容差登記表，尤其 T60 比值列） | 驗收紅線 |
| `src/physics/StringModel.h` | 全文，尤其 `decayTimeForFrequency`（46–57 行）、`calculateModes`（63–約 150 行） | 要改的物理核心，**以工作樹實際內容為準，不要憑本卡的行號想像** |
| `src/physics/MaterialDB.h` | 全文，尤其 `Material`/`Damping` 結構（66–73 行）、class 頂端 schema 註解（6–29 行） | `youngsModulus`/`poissonRatio`/`density` 欄位名稱與現有材質資料 |
| `src/engines/CimbalomEngine.h` | 全文，尤其 `CimbalomVoice::startNote()`（約 99–265 行）與 `CimbalomVoice::noteOn()`（約 296–444 行）兩條渲染路徑，以及檔頭常數區（約 25–33 行） | 要接線的兩條路徑 |
| `src/score/ScoreRenderer.h` | 所有呼叫 `voice.noteOn(...)` / `voice->noteOn(...)` 的地方（用 grep 找，見 §3） | `noteOn()` 簽名一改，這裡全部要跟著改 |
| `docs/workcards/B2.md` | 全文（尤其 §1、§3、§9、§11） | B2 對「B1 完工後長什麼樣」的假設，本卡不能跟它矛盾 |

---

## 3. 要改的檔案與函式

### 3.1 `src/physics/StringModel.h`

- **新增** `static float bridgeLossRate(float tension, float length, const MaterialDB::Material& soundboardMaterial, float soundboardThicknessM)`
  純公式函式（不摸 `MaterialDB::Material` 以外的任何全域狀態），算出
  `T·G/(ln(1000)·L)`。公式見 §4。
- **修改** `decayTimeForFrequency(...)`：簽名新增第 4 個參數
  `float bridgeLoss = 0.0f`（預設 0 = 不加這項，保證舊呼叫端不傳這個參數時
  行為位元不變——但正式渲染路徑 `CimbalomEngine.h` 一定要傳非零值，見 3.2）。
  `denominator` 加上 `+ bridgeLoss`。
- **更新**函式最上方的 doc comment（目前 33–45 行），把公式從三項改寫成四項，
  註明第四項來源＝`docs/BRIDGE_ADMITTANCE_SOURCES.md`。
- **不改** `calculateModes()`（約 113 行）內部那次 `decayTimeForFrequency(freq, material)`
  呼叫——它拿不到共鳴板資訊（`calculateModes` 的參數只有 `Params`+`Material`，
  不含音板），維持三項即可，因為 `CimbalomEngine.h` 兩條渲染路徑事後**都會**
  用四項版本覆寫 `m.decayTime`（見 3.2）。**這是工兵必須驗證的假設，不是可以
  跳過的細節**——用 §6 步驟 2 的 grep 指令確認兩條路徑真的都覆寫了。

### 3.2 `src/engines/CimbalomEngine.h`

- **新增兩個檔案作用域常數**，放在現有 `kMaxStringsPerCourse`/`kCimbalomAttackEnergyRefA4`
  旁邊（目前約 25–33 行）：`kBridgeSoundboardThicknessM`、
  `kBridgeSoundboardMaterialKey`。數值、來源、待裁決標記見 §5。
- **`CimbalomVoice::startNote()`**（JUCE 即時演奏路徑，約 99–265 行）：
  - 在解析出弦材質 `mat`（約 108 行 `materialDB->getMaterial(...)`）之後，
    **再解析一次共鳴板材質**：`auto* soundboardMat = materialDB->getMaterial (kBridgeSoundboardMaterialKey);`，
    fail-closed 比照上一行同樣的 `if (nullptr) return;`（materialDB 本來就必須
    含 `wood_spruce`，這個 guard 理論上不會觸發，但要寫，不准省略）。
  - 建好 `StringModel::Params sp`（約 139–145 行）之後，**只算一次**：
    `const float bridgeLoss = StringModel::bridgeLossRate (sp.tension, sp.length, *soundboardMat, kBridgeSoundboardThicknessM);`
  - 把 `bridgeLoss` 傳進**兩處**既有的 `decayTimeForFrequency` 呼叫（攻擊能量
    預估迴圈約 199 行、per-string 最終迴圈約 247 行），呼叫變成
    `StringModel::decayTimeForFrequency (m.frequency, *mat, -1.0f, bridgeLoss)`。
    **兩處都要改，只改一處會讓響度預估（`noteComp`）跟實際渲染的衰減律不一致，
    這正是 199/247 行附近既有註解反覆強調的「預估與實際渲染必須用同一條衰減律」，
    歷史上（2026-08-06 那輪）就是這個不一致造成 tongue_drum velocity 律違規。**
- **`CimbalomVoice::noteOn()`**（Standalone/CLI 渲染路徑，約 296–444 行）：
  - 這條路徑的 `mat` 是**呼叫端**（`ScoreRenderer.h`）解析好、以 `const MaterialDB::Material&`
    傳進來的，`materialDB` 這個 member 在這條路徑**沒有被設定**（只有 `startNote()`
    那條路徑會呼叫 `setMaterialDB()`；用 grep `materialDB` 確認這個事實，不要假設）。
    因此**必須修改 `noteOn()` 的簽名**，新增一個參數：
    ```cpp
    void noteOn (int midiNote, float velocity,
                 const MaterialDB::Material& mat,
                 const MaterialDB::Material& soundboardMat,   // 新增
                 const CimbalomParams& params)
    ```
  - 函式內部：`sp`（約 313–321 行）建好之後算一次 `bridgeLoss`（同 3.2 上段公式），
    傳進兩處既有呼叫（約 392 行的攻擊能量預估迴圈、約 415–416 行的 per-string
    最終迴圈），保持該路徑原有的 `dampingOverride` 參數不變，只加第 4 個位置參數。
- **註解更新**：`noteOn()` 附近（約 363–375 行）解釋 `dampingOverride` 語意的
  註解說「`beta_air`/`gamma_radiation` 永遠 material-driven、不受 override 影響」——
  加一句「橋耦合項（`bridgeLoss`）同理，永遠疊加，不受 `damping_override` 影響」。
  `StringModel.h` 與 `MaterialDB.h` 裡類似措辭的地方（`etaFromAnchoredDamping`
  附近）也要同步這句話。

### 3.3 `src/score/ScoreRenderer.h`（因為 3.2 改了 `noteOn()` 簽名，這裡全部要跟著改）

先跑：
```
grep -n "voice.noteOn\|voice->noteOn" src/score/ScoreRenderer.h
```
把結果當作權威清單（本卡寫作時是 209 / 281 / 709 / 808 / 1007 / 1051 六處，但
到你動手時行號可能已經漂移，**以你自己跑出來的結果為準**）。**每一處**都要：

1. 在既有解析字串材質 `mat`（例如 `materialDB->getMaterial (juce::String (ev.material))`）
   的**同一段程式碼**旁邊，加一行解析共鳴板材質：
   `materialDB->getMaterial (kBridgeSoundboardMaterialKey)`（這個常數在
   `CimbalomEngine.h` 裡宣告為檔案作用域，`ScoreRenderer.h` 已 `#include` 該檔，
   直接可用；如果編譯器抱怨找不到符號，改成完整寫法
   `CimbalomVoice::kBridgeSoundboardMaterialKey`——實際能不能省略類別限定看
   你怎麼宣告它，兩種都合法，選一種、全檔一致）。
2. **失敗處理比照該處既有對 `mat` 的處理方式**——不要自己發明新的失敗行為。
   已知這 6 處至少有兩種既有寫法：有的是 `if (mat==nullptr) continue;`（跳過整個
   event），有的是 `if (mat==nullptr) mat = materialDB->getMaterial("steel");`
   再退一步判 `nullptr`。共鳴板材質查詢一律比照**該處**既有寫法，不要混用。
3. 把解析到的共鳴板材質傳進對應的 `noteOn(...)` 呼叫（新增的第 4 個實參，
   在 `mat` 之後、`cp`/`params` 之前）。

### 3.4 明確不動的檔案

- **`src/engines/ChromaticEngine.h`／`src/physics/BeamModel.h`／`src/physics/PlateModel.h`
  一行都不改。** §2.2 的推導（弦端反射係數→振幅衰減率）是針對行波弦做的，
  梁與板要各自重新推導，`BRIDGE_ADMITTANCE_SOURCES.md` §5 明寫「本輪不推」。
  Tongue Drum／Water Gong 的阻尼律維持現狀（寬頻化後的三項，無第四項）。
- **`data/materials.json` 不新增欄位。** 共鳴板材質直接重用既有的 `wood_spruce`
  項（已有 `density`/`youngs_modulus`/`poisson_ratio`，算 `D`/`ρs` 夠用）。
  **但要在 `MaterialDB.h` 檔頭 schema 註解（6–29 行）補一段話**，說明
  `wood_spruce` 現在有兩種語意（弦材質候選 + 橋耦合預設共鳴板參照材質），
  互不影響但要讓後來的人知道——這是 `docs/RESEARCH_INDEX.md` §6 已預見的
  「同一份 materials.json 對不同引擎語意不同」問題的同類情形，比照 B3 卡片
  預計要做的 Rule 9 標註方式處理，先用一句註解佔位。

---

## 4. 公式與常數

逐條對應 `docs/BRIDGE_ADMITTANCE_SOURCES.md` §2.1–§2.3，**照抄不要重新推導**：

```
D    = E·h³ / (12·(1−ν²))          共鳴板彎曲剛度 (N·m)
rhoS = ρ·h                          共鳴板面密度 (kg/m²)
Y∞   = 1 / (8·√(D·rhoS))            無限板驅動點導納 (s/kg)，純實數、與頻率無關
G    = Re(Y∞) = Y∞                  （虛部不存在，這個量本來就是實數）
α    = (T/L)·G                      弦端振幅衰減率 (1/s)
1/T60_bridge = α / ln(1000) = T·G / (ln(1000)·L)
ln(1000) = 6.907755278982137        與 tools/physics_verify.py 的 MODAL_DECAY_LN1000、
                                     ModalResonator::excite() 的 6.9078f 同一常數
```

**R4 溯源註解範本**（直接抄到 `StringModel::bridgeLossRate` 上方）：

```cpp
/** Frequency-independent bridge/soundboard coupling loss-rate contribution
 *  to 1/T60 -- energy flowing from the string end, through the bridge, into
 *  the soundboard. Closed-form literature result, not a fitted constant.
 *  Full derivation chain: docs/BRIDGE_ADMITTANCE_SOURCES.md §2.1-§2.3.
 *
 *  D    = E*h^3 / (12*(1-nu^2))     soundboard bending stiffness (N*m)
 *         [Cremer, Heckl & Ungar, "Structure-Borne Sound"; classic
 *          thin-plate bending-stiffness result]
 *  rhoS = rho*h                     soundboard areal density (kg/m^2)
 *  Y_inf = 1 / (8*sqrt(D*rhoS))     infinite-plate driving-point admittance
 *         (s/kg), REAL and frequency-independent
 *         [Cremer/Heckl/Ungar + Skudrzyk mean-value theorem; algebraically
 *          verified equal to Ege & Boutillon's Y_C = (1/(4h^2))*sqrt(3*(1-nu^2)/(E*rho)),
 *          see BRIDGE_ADMITTANCE_SOURCES.md §2.1]
 *  G    = Re(Y_inf) = Y_inf         (already real-valued; imaginary part
 *         dropped by construction -- see BRIDGE_ADMITTANCE_SOURCES.md §4
 *         point 3, no detuning contribution modeled here)
 *  alpha = (T/L)*G                  string-end amplitude decay rate (1/s)
 *         [derived from the reflection coefficient at a bridge terminated
 *          by admittance Y; cross-checked against Chaigne ICA 2010's
 *          alpha_n = (T/L)*G(omega); see BRIDGE_ADMITTANCE_SOURCES.md §2.2]
 *  1/T60_bridge = alpha / ln(1000) = T*G / (ln(1000)*L)
 *         ln(1000) = 6.907755278982137, same constant as
 *         tools/physics_verify.py's MODAL_DECAY_LN1000 and
 *         ModalResonator::excite()'s 6.9078f literal.
 *
 *  Deliberately NO coupling-reduction fudge factor here (Rule 4): adding
 *  one would turn a traceable closed-form result into an untraceable
 *  tunable knob. See BRIDGE_ADMITTANCE_SOURCES.md §5.
 *
 *  @param tension              string tension T (N)
 *  @param length                string length L (m)
 *  @param soundboardMaterial    soundboard material (NOT the string material)
 *  @param soundboardThicknessM  soundboard thickness h (m); must be > 0
 *  @return  1/T60 contribution (1/s). Returns 0.0f (fail-closed: no
 *           contribution, NOT a crash/NaN/negative-T60) if any input is
 *           non-finite or non-positive.
 */
```

`decayTimeForFrequency` 的 4 個引數也各補一句 R4 出處註解，指回上面這個函式。

---

## 5. 新增參數/schema

**不新增 score.json schema 欄位。** 兩個新常數是引擎層級的編譯期常數，不開放給
樂譜作者調整（原因：數值本身還沒經月月確認，開放成可調參數等於在數值未定案時
先固化一個 schema，之後要改會動到樂譜相容性）。

| 常數 | 型別 | 值 | 預設值來源 | 待裁決 |
|---|---|---|---|---|
| `kBridgeSoundboardThicknessM` | `float`（`constexpr`） | `0.009f`（9 mm） | `BRIDGE_ADMITTANCE_SOURCES.md` §5：文獻只給「鋼琴音板 8–10 mm」，取中點。**這是鋼琴文獻值,不是 TsukiSynth cimbalom 的實測值** | ✅ 是，見下方 TODO 登記 |
| `kBridgeSoundboardMaterialKey` | `const char*` | `"wood_spruce"` | `materials.json` 既有項；鋼琴/揚琴音板慣用雲杉，但這是類比選擇不是實測 | ✅ 是，見下方 TODO 登記 |

**必須做**：在 `TODO.md` 開頭「待辦總表」A 段（等月月決定）新增一項（接續現有
A1–A10，本卡新增 **A11**）：

```markdown
- [ ] **A11 共鳴板厚度 h 與材質選定（B1 琴橋導納）** —— 目前實作用
      `h = 9mm`（文獻「鋼琴音板 8–10mm」範圍中點）、材質 = `wood_spruce`
      （`materials.json` 既有項，鋼琴/揚琴音板慣用雲杉的類比選擇）。
      兩者皆非 TsukiSynth cimbalom 的實測值，是暫定的文獻類比預設值
      （`docs/BRIDGE_ADMITTANCE_SOURCES.md` §5）。需月月確認是否合理，
      或改用其他材質/厚度。
```

**fail-closed 行為**：
- `bridgeLossRate()` 收到非有限或非正的輸入（`h<=0`、`length<=0`、`tension`
  非有限、算出的 `D`/`rhoS`<=0）→ 回傳 `0.0f`（等於這個損耗通道不存在，
  退回三項阻尼律的行為），**不是** NaN、不是負數、不是讓 `decayTimeForFrequency`
  的 denominator 變成非正數。
- `materialDB->getMaterial(kBridgeSoundboardMaterialKey)` 查不到（理論上不該
  發生，`wood_spruce` 本來就在 `materials.json`）→ 比照該呼叫點既有對字串材質
  查詢失敗的處理方式（見 §3.2/§3.3），一律是「跳過這個音符/事件」，不是
  「靜默地當作沒有橋耦合繼續播放」。

---

## 6. 實作步驟

1. 確認前置狀態（§1 第 2 點）。
2. 用 grep 驗證 §3.1 的假設：
   ```
   grep -n "StringModel::calculateModes\|StringModel::decayTimeForFrequency" src/engines/CimbalomEngine.h
   ```
   目前應該看到 `calculateModes` 呼叫 2 次、`decayTimeForFrequency` 呼叫 4 次
   （`startNote()` 2 次 + `noteOn()` 2 次）。若數字不同，代表工作樹已經跟本卡
   寫作時不一樣，重新確認每一處的用途再繼續，不要照抄本卡預期的次數硬做。
3. 在 `StringModel.h` 新增 `bridgeLossRate()`（§3.1、§4），新增
   `kLn1000`（`6.907755278982137f`，可以是 `StringModel` 內的
   `static constexpr float`，命名避免跟 `MaterialDB::kEtaToDecayRate` 混淆）。
4. 修改 `decayTimeForFrequency()` 簽名與 denominator（§3.1）。更新其上方 doc
   comment。
5. 在 `CimbalomEngine.h` 加兩個新常數（§3.2、§5），含 R4 + 待裁決註解。
6. 改 `startNote()`：加共鳴板材質解析、算 `bridgeLoss`、兩處呼叫都傳
   （§3.2）。**做完立刻重讀一次這兩處，逐字確認兩個呼叫的 `bridgeLoss` 引數
   來自同一個變數**（不是各自重新算一次——重新算理論上數值會一樣，但這是
   在製造未來被改壞的地雷,直接共用同一個區域變數）。
7. 改 `noteOn()`：簽名加 `soundboardMat` 參數、算 `bridgeLoss`、兩處呼叫都傳
   （§3.2）。
8. 用 `grep -n "voice.noteOn\|voice->noteOn" src/score/ScoreRenderer.h` 找出
   全部呼叫點，逐一補上共鳴板材質解析與新實參（§3.3）。**這一步做完要看到
   什麼**：`grep -c "kBridgeSoundboardMaterialKey" src/score/ScoreRenderer.h`
   的數字應該等於呼叫點數量（每處恰好一次解析），不多不少。
9. 更新 §3.2 提到的三處 `dampingOverride` 語意註解（加一句橋耦合項不受
   override 影響）。
10. `MaterialDB.h` 檔頭補一句 `wood_spruce` 雙重語意的說明（§3.4）。
11. 全域搜尋確認 `ChromaticEngine.h`/`BeamModel.h`/`PlateModel.h` 零改動：
    ```
    git diff --stat src/engines/ChromaticEngine.h src/physics/BeamModel.h src/physics/PlateModel.h
    ```
    必須是空輸出（這三個檔案在這次 diff 裡完全不出現，或即使出現也應該是
    2026-08-10 那批既有 unstaged 改動、不是本卡新增的行——用
    `git diff` 而非 `git status` 才看得出「新增了什麼」）。
12. 寫 §7 的新測試（`tests/physics_models_repro.cpp`）。
13. 建置與測試（§8 GATE 清單全部跑一次，第一輪）。
14. 若第一輪有任何 FAIL：修正、**不准放寬任何容差**，回到步驟 13 重跑，直到
    全綠或確認卡在 §12 描述的情況需要停下。
15. 更新 `TODO.md`：B 段 B1 checkbox 打勾；新增 A11（§5）。
16. 更新 `ROADMAP_PHYSICS.md` §2：新增一列 **M10 琴橋導納／共鳴板耦合
    （頻率無關損耗通道，弦域）**，狀態 `Done`，內容比照其他列的寫法（一段話
    講清楚做了什麼、GATE 證據路徑、明確寫「本 milestone 範圍不含 corpus 全量
    重驗與響度錨點常數重測——那是後續 B2 卡片的範圍」，避免跟 Rule 5 衝突）。
17. 檔案留 unstaged，不 commit、不 push。

---

## 7. 測試要新增什麼

檔案：`tests/physics_models_repro.cpp`，接續現有 eta 寬頻化測試區塊之後
（約 143–173 行那一段，風格照抄：`MaterialDB::Material` 建構 + `CHECK(...)`）。

1. **`Y_inf` 公式對照文獻手算值**（正例）
   用 `docs/BRIDGE_ADMITTANCE_SOURCES.md` §2.1 表格的**原始輸入**
   （`E=11.5e9, rho=400, nu=0.30`——這是文件自己驗算用的合成材質，**不是**
   `materials.json` 的 `wood_spruce`，兩者數值不同，不要混用）建一個
   `MaterialDB::Material` stub，呼叫 `bridgeLossRate()` 內部同一條 `Y_inf`
   算式（或直接呼叫 `bridgeLossRate` 並反推,只要能對照到 `Y_inf` 本身），
   確認 `h=8mm → Y_inf≈3.01e-3 s/kg`、`h=10mm → Y_inf≈1.93e-3 s/kg`
   （容差 ±0.5%，文件表格只給 3 位有效數字）。

2. **`T60_bridge` 對照文獻表**（正例，交叉驗證代數鏈整體，不經過 `Y_inf`）
   用 §3 表格的「逐弦 T/L」欄位當作 `tension`（`length=1.0f`，因為只需要
   `T/L` 這個比值）、`G=1.3e-3 s/kg` 直接當作已知量（繞過 `bridgeLossRate`
   內部的 `Y_inf` 計算，改用等價的 `T*G/(ln1000*L)` 直接驗算），確認：
   - C2（T/L=1250.0）→ T60_bridge ≈ 4.25 s
   - C4（T/L=1073.3）→ T60_bridge ≈ 4.95 s
   - C8（T/L=12650.0）→ T60_bridge ≈ 0.42 s
   容差 ±1%。這條測試證明的是「`α→T60` 那段代數轉換沒寫錯」，不是驗證
   `Y_inf` 公式本身（那是測試 1 的工作）。

3. **頻率無關性**（正例，比照現有 143–165 行 eta 廣頻測試的風格）
   建一個 `eta=beta_air=gamma_radiation=0` 的材質，`bridgeLoss` 給非零值，
   確認 `decayTimeForFrequency(55.0f, mat, -1, bridgeLoss)` 與
   `decayTimeForFrequency(4000.0f, mat, -1, bridgeLoss)` **完全相等**（浮點
   容差極小，這條路徑理論上應該位元級相同，因為 `bridgeLoss` 是常數輸入不
   依賴 frequency 參數）。

4. **反例／哨兵（必須）**：證明測試 3 真的有偵測力，不是恆真
   在測試檔案裡（不動 `StringModel.h` 本體）臨時寫一個**故意錯的**
   替身版本，例如 `float brokenBridgeLoss = bridgeLoss * (freq / 261.6256f);`
   （刻意做成隨頻率變化），把它當「橋損耗項」代入同一個比較邏輯，確認
   兩個頻率算出的值**不相等**——證明測試 3 的比較邏輯真的能抓到「有人不小心
   把橋耦合項寫成頻率相關」這種回歸。過程與兩次結果（壞版本 FAIL、正確版本
   PASS）存到 `reports/gate_outputs/b1_selftest_sentinel.txt`（比照
   `physics_verify.py --selftest`／`docs/workcards/B2.md` §7 的哨兵測試手法）。

5. **`damping_override` 與橋耦合共存**（正例，回歸既有語意）
   確認 `dampingOverride>=0` 時，`internalFrictionRate` 那一項被換成
   override 換算出的等效值，但 `bridgeLoss` 照樣加進 denominator——即
   `decayTimeForFrequency(f, mat, dampingOverride, bridgeLoss)` 的結果
   ≠ `decayTimeForFrequency(f, mat, dampingOverride, 0.0f)`（兩者應該不同，
   證明 `bridgeLoss` 沒有被 override 路徑意外吃掉）。

6. **向下相容**（正例）
   確認不傳第 4 個參數（用舊的 3 參數呼叫）的結果，跟傳 `bridgeLoss=0.0f`
   完全一樣——保證任何本卡沒改到的既有呼叫點行為不變。

7. **fail-closed 反例**：`bridgeLossRate()` 傳入 `soundboardThicknessM=0.0f`
   或負值，確認回傳 `0.0f`（不是 NaN/Inf/負數）。同樣測 `length<=0`。

每條測試都要有清楚的 `CHECK(..., "說明字串")`，字串要講清楚驗的是哪個性質
（照抄現有檔案的風格）。

---

## 8. GATE 清單

R6 規定的兩項是最低要求（`--full` + 三 target build），本卡另外加上新測試的
build+跑測試證據，以及一次不追求覆蓋率、只確認方向正確的 T60 抽樣：

> **【X4 規約・2026-08-24 事後補記】** 本卡已完工，此段為事後補記（本卡施工時
> 尚無此規約，且正是本卡踩坑後才立的規）：跑任何 `ctest` 之前，必先重建三個
> 測試 target：
> `cmake --build build --config Release --target TsukiSynthAuditTest TsukiSynthTunerTest TsukiSynthPhysicsModelsTest`，
> 否則會測到未重建的舊 binary——2026-08-16 本卡實際踩過：`b1_ctest_all.txt`
> 的「3/3 passed」是測到舊 binary 的假綠燈。

| # | 命令 | 期望輸出 | 存檔路徑 |
|---|---|---|---|
| 1 | `cmake --build build --config Release --target TsukiSynthPhysicsModelsTest` | exit 0（含本卡新增的 §7 測試） | `reports/gate_outputs/b1_build_test.txt` |
| 2 | `ctest --test-dir build -C Release -R physics_models_repro --output-on-failure` | Passed，0 Failed（§7 全部 6+ 條新測試都在輸出裡看得到） | `reports/gate_outputs/b1_ctest.txt` |
| 3 | 見 §7 測試 4 的哨兵手法（先破壞、再修正） | 破壞版 FAIL → 修正版 PASS，兩次都存證 | `reports/gate_outputs/b1_selftest_sentinel.txt` |
| 4 | `python tools/physics_verify.py --full` | `RESULT: NO CHECKED FAILURES`（Rule 6 強制項，全引擎，不准只跑 cimbalom） | `reports/gate_outputs/b1_gate_full.txt` |
| 5 | `python tools/physics_verify.py --t60 --notes 21 33 45 57 69 81` | 方向抽查：steel cimbalom/piano 的 T60 應該**明顯短於**
`damping_broadband_findings.md` §3.1 記錄的舊數字（例如 MIDI 33≈C2 附近不該
再是 ~129s 量級），量級應該落在個位數~十位數秒（`BRIDGE_ADMITTANCE_SOURCES.md`
§3 的 combined T60 欄是參考量級，不要求精確匹配——精確匹配是 B2 的工作）。
**不是** exit-code 判定的正式 GATE（那是 `--full` 涵蓋的），這裡只是人工核對
方向，不追求覆蓋率 | `reports/gate_outputs/b1_t60_direction_check.txt` |
| 6 | `cmake --build build --config Release --target TsukiSynthCLI` | exit 0 | `reports/gate_outputs/b1_build_cli.txt` |
| 7 | `cmake --build build --config Release --target TsukiSynth_Standalone` | exit 0 | `reports/gate_outputs/b1_build_standalone.txt` |
| 8 | `cmake --build build --config Release --target TsukiSynth_VST3` | exit 0 | `reports/gate_outputs/b1_build_vst3.txt` |
| 9 | `ctest --test-dir build -C Release --output-on-failure` | 全數 Passed，0 Failed（三個既有 C++ target 全跑，不是只跑 physics_models_repro） | `reports/gate_outputs/b1_ctest_all.txt` |

**明確排除在本卡 GATE 之外、留給 `docs/workcards/B2.md` 做的事**（不是縮小
Rule 3 的 GATE 範圍，是兩張卡分工，B2 §1 會自己驗證 B1 是否真的做完）：
corpus 73 檔全量重驗（`verify_score.py --all`）、`kCimbalomAttackEnergyRefA4`
等響度錨點常數重測、Rule 10 合併前後對照報告
（`reports/b1_b2_bridge_damping_before_after.md`）。**不要在本卡裡動
`kCimbalomAttackEnergyRefA4` 的數值**——那個常數的重新量測方法與時機是
B2 卡片 §6 步驟 8 明確規劃好的,本卡改了會讓 B2 的「前後」歸因混亂。

---

## 9. Rule 10 前後對照報告要包含什麼

**本卡不產出獨立的 Rule 10 前後對照報告。** `docs/workcards/B2.md` §9 已經
規劃好一份**合併**報告（`reports/b1_b2_bridge_damping_before_after.md`），
明確要求「不要分開寫兩份」。本卡的責任只是：

- §8 表格第 5 項（T60 方向抽查）的存證檔案要留著、內容要看得出「改動前後」
  的量級差異（可以是同一次命令輸出裡對照 `damping_broadband_findings.md`
  §3.1 的舊數字，不需要另外渲染一輪舊版音檔——B2 會做完整版的前後渲染對照）。
- §6 步驟 16 更新 `ROADMAP_PHYSICS.md` 時，在 M10 該列註明「本改動會讓所有
  Cimbalom/Piano score 的渲染結果改變（衰減變快），完整的 Rule 10 前後對照
  由 B2 卡片產出」，讓月月知情但不誤以為本卡自己交了對照報告。

---

## 10. 完成的定義

以下**全部**滿足才算 Done：

- §3.1–§3.4 所有檔案改動完成，且 §3.4 明確不動的三個檔案零改動（§6 步驟 11
  驗證通過）。
- §7 全部測試（至少 6 條，含 §7-4 的哨兵反例）新增完成，`ctest` 全過。
- §8 GATE 清單 9 條全部跑過，輸出符合期望，證據檔全部存在。
- `TODO.md` B 段 B1 checkbox 打勾，新增 A11（§5）。
- `ROADMAP_PHYSICS.md` §2 新增 M10 列，狀態 `Done`，內容含 GATE 證據路徑
  與「corpus/響度錨點不在本 milestone 範圍」的邊界說明。
- `noteOn()` 簽名改動後，`src/score/ScoreRenderer.h` 全部呼叫點（§3.3 grep
  出來的清單）都已更新，且 §6 步驟 8 的計數檢查（grep count 對得上呼叫點數）
  通過。
- 檔案留 unstaged，未 commit、未 push。

任一項不滿足 → 標記 `In progress`，列出剩餘項目（哪一步卡住、哪一條 GATE
沒過）。**不准把「大部分呼叫點改完、剩一兩個」寫成 Done**——`noteOn()`
簽名改一半會直接編譯失敗，不存在「部分完成」這種中間態，要嘛全部呼叫點
都跟上新簽名要嘛不編譯。

---

## 11. 已知風險與地雷

- **`noteOn()` 簽名改動是本卡最大的技術風險。** §3.3 列出的呼叫點清單是本卡
  寫作當下（2026-08-16）的快照，實際數量與行號到你動手時可能已經漂移
  （尤其如果 B2 或其他工作先落地了）。**用 grep 自己重新枚舉，不要相信本卡
  寫死的行號**。漏改一處 = 編譯失敗（好事，會立刻被抓到）；但如果漏改的
  那處剛好用了不同的失敗慣例（§3.3 點 2 提到至少兩種既有寫法），照抄錯誤的
  慣例會導致某些 event 的共鳴板材質查詢失敗處理不一致。
- **估能量迴圈與最終渲染迴圈必須用同一個 `bridgeLoss` 變數**（§3.2、§6 步驟
  6）。2026-08-06 那輪能量正規化就因為兩處用了不一致的衰減假設，導致
  tongue_drum velocity 律 +4.72 dB 違規（`TODO.md` 2026-08-06 條目有記錄）。
  這條路以前栽過跟頭，本卡直接繼承同一個風險點。
- **不要把 `kCimbalomAttackEnergyRefA4` 一起改了。** 這個常數理論上會因為
  T60 變短而需要重新量測，**但那是 `docs/workcards/B2.md` 明確規劃的範圍**
  （見該卡 §3、§6 步驟 8）。本卡如果順手改了它，會讓 B2 的「量測 B1 落地後的
  新數字」這個前提失真（B2 §1 會檢查這個常數有沒有被 B1 動過嗎？不會，但
  改了它會讓 B2 產出的「舊值→新值」對照報告失去意義）。**本卡完工時
  `kCimbalomAttackEnergyRefA4` 應該還是 `0.1497f`，一個字元都不動。**
- **`docs/BRIDGE_ADMITTANCE_SOURCES.md` §3 自己承認「單一平均 G 對 C3 仍偏短
  2.4 倍」**——這是公式本身（量級核對而非精確驗證）的已知侷限，不是本卡的
  實作缺陷。§8 第 5 項的 T60 方向抽查只要求「量級對、方向對」，不要求跟
  Wogram 實測精確吻合，也不准為了讓 T60 更貼近實測而調整 `G` 或加係數
  （那正是 R4 明文禁止的「無法溯源的耦合折減係數」）。
- **`wood_spruce` 雙重語意**：`STRING_DAMPING_SOURCES.md`／`RESEARCH_INDEX.md`
  已經預見 B3 卡片會讓同一份 `materials.json` 對弦引擎與板/梁引擎有不同語意；
  本卡額外疊加了第三種語意（`wood_spruce` 同時可能被選為弦材質，也是橋耦合
  的預設共鳴板材質）。這不是 bug，但如果之後有人把 cimbalom 的弦材質也設成
  `wood_spruce`（木弦？），橋耦合公式會用**同一個** `wood_spruce` 物件算
  「弦端張力」（來自 `sp.tension`／`sp.length`，跟材質選擇無關）和「共鳴板
  彈性」（來自共鳴板材質的 `E`/`ρ`/`ν`），兩者物理上不衝突，只是巧合用了
  同一份 JSON 條目，不需要特殊處理，但容易在 code review 時被誤會成 bug，
  §3.4 要求補的那句註解就是為了防這個誤會。
- **浮點動態範圍**：`D = E·h³/(12(1-ν²))` 對 `wood_spruce`
  （`E=12e9`, `h=0.009`）算出來量級是 `12e9 * 7.29e-7 / 12 ≈ 730`（N·m），
  `rhoS = 450*0.009 = 4.05`（kg/m²），`D·rhoS ≈ 2957`，`sqrt ≈ 54.4`，
  `Y_inf ≈ 1/(8*54.4) ≈ 2.3e-3`——這些數字都在 float32 安全範圍內，不需要
  特別的 double 精度處理，但如果之後有人把 `h` 改成極端值（例如誤填成
  `9.0f`，忘記單位是公尺不是公厘），`D` 會暴增到 `1e18` 量級,`Y_inf` 崩潰到
  接近 0，T60_bridge 趨近無限大（等於沒有這個損耗項），**這種輸入錯誤不會
  觸發任何一個 fail-closed 檢查**（`h=9.0f` 是有限正數，通不過 `h<=0` 的
  guard）。本卡不強制加額外的合理範圍檢查（那會是另一個需要溯源的「什麼算
  合理範圍」的常數），但如果 §7 或 §8 的數字明顯離譜（T60 幾乎沒變），第一
  個該懷疑的就是單位錯誤，不是公式錯誤。

---

## 12. 做不到時怎麼辦

- **§1 前置檢查失敗**（工作樹狀態跟本卡假設不符）：停下，不做任何改動，回報
  實際讀到的 `decayTimeForFrequency` 內容（檔案路徑+行號+片段），不要猜測
  月月的意圖後自行決定要不要繼續。
- **`noteOn()` 呼叫點數量或行為模式跟 §3.3 描述的不一樣**（例如發現超過 2
  種失敗慣例，或某處根本沒有既有的材質查詢失敗處理）：停下,列出你實際看到
  的每一處的現況與差異,回報,不要自己發明一個「看起來合理」的統一寫法硬套
  上去。
- **§7 測試 1／2（文獻數字對照）算出來的數字跟文件對不上，超出容差**：
  停下。回報實測數字、文件數字、差距百分比,附上你的計算過程（哪個中間值
  算出來多少）。**不准**調整公式係數、不准調整 `h`/材質數值去湊出文件數字——
  文件的數字是獨立算出來的參照值,如果對不上,問題可能在你的實作,也可能在
  文件本身有誤,兩種都需要月月/人工判斷,不是工兵能自行決定往哪邊修的。
- **§8 GATE 清單任何一項 FAIL**（尤其 `--full` 出現 checked failure）：
  回報完整命令輸出、具體是哪個引擎/哪個判定項、實測數字與 §6 容差登記表
  對應列的門檻差多少。**不准修改 `ROADMAP_PHYSICS.md` §6 的任何容差數值**。
- **編譯失敗**（`noteOn()` 簽名改動漏改某個呼叫點）：這是預期中會發生、
  也應該發生的事（證明你漏改了東西）,直接修正,不是「做不到」的情況,不需要
  停下回報,除非你已經找不到還有哪裡沒改（用 §6 步驟 8 的 grep count 檢查
  法定位）。
- 一律附實測數字與命令輸出路徑,不得只寫「大致正常」「應該沒問題」這類敘述。
